# facecook-fe — 기여 가이드

정리 기준일: 2026-09-03

이 레포는 페이스콕(제52회 용마대동제 소개팅 부스) 서비스의 **프론트엔드 전용**
레포다. 백엔드는 `facecook-be`(Spring Boot)로 분리되어 있다.

## 브랜치 전략

- `main`에서 기능별 브랜치를 파생하는 단순 구조 (`main` ← `feat/기능명` 형식).
  별도 통합용 `dev` 브랜치는 두지 않는다
- 새 브랜치는 항상 **최신 `main`**에서 생성한다
- 브랜치명: `{타입}/{작업내용}` — 영문 소문자, 단어는 하이픈(`-`)으로 구분
  - 예: `feat/chat-ui-polish`, `fix/kok-tab-state`, `docs/onboarding-update`
- `main`에 직접 push하지 않는다. 작업이 끝나면 PR을 올려서 머지한다

## 커밋 메시지 컨벤션

형식: `타입: 작업 내용`

| 타입 | 사용 시점 |
| --- | --- |
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 추가/수정 (코드 변경 없음) |
| `ref` | 동작 변화 없는 코드 구조 개선(리팩터링) |
| `chore` | 빌드 설정, 패키지 업데이트, 그 외 잡무성 변경 |

예시:

```
feat: 채팅 메시지 전송 UI 추가
fix: 콕 화면 탭 전환 시 상태 초기화 버그 수정
docs: README 실행 방법 갱신
ref: 프로필 카드 컴포넌트 분리
chore: eslint 설정 업데이트
```

하나의 커밋에 여러 타입이 섞인 작업은 가능하면 커밋을 나눈다. 안 되면 가장
비중이 큰 타입을 쓴다.

## PR 규칙

- `main`에 직접 push 금지, 반드시 PR을 통해서만 머지
- 머지 전 로컬에서 정상 동작(빌드/실행) 확인
- 리뷰어 없이 셀프 머지는 지양하되, 리뷰어 확보가 어려우면 최소한 PR 설명에
  변경 내용을 명확히 남긴다

## 이 레포의 범위

`ui/`, `src/`는 실제 화면과 라우팅만 담당한다. 백엔드 로직(인증, DB 접근 등)을
이 레포에 다시 넣지 않는다 — 그건 `facecook-be`의 책임이다.

## 이 문서 범위 밖

- 이슈 템플릿, 라벨 규칙은 필요해지면 별도로 추가한다
- 커밋 메시지 포맷을 자동 검증하는 도구(husky 등)는 지금은 두지 않는다
